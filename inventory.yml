vagrant_machines:
  hosts:
    ael-yamoS:
      ansible_host: "{{secrets.host1.ip}}"
      ansible_user: "{{secrets.host1.user}}"
      ansible_ssh_private_key_file: "{{secrets.host1.private_key_file}}"
    # second-yamo:
    #   ansible_host: "{{secrets.host2.ip}}"
    #   ansible_user: "{{secrets.host2.user}}"
    #   ansible_ssh_private_key_file: "{{secrets.host2.private_key_file}}"